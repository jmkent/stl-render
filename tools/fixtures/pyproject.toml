[project]
name = "stl-fixtures"
version = "0.1.0"
description = "Generate STL test fixtures for stl-render"
requires-python = ">=3.10"
dependencies = [
    "numpy-stl>=3.1.0",
    "numpy>=1.24.0",
]

[project.scripts]
generate-fixtures = "generate_fixtures:main"
